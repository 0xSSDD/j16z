---
phase: 05-alert-delivery-market-data
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - apps/api/src/routes/alert-rules.ts
  - apps/api/src/index.ts
  - apps/api/src/agency/event-factory.ts
  - apps/api/src/edgar/download.ts
  - apps/api/src/courtlistener/webhook.ts
  - apps/api/src/rss/poller.ts
  - apps/j16z-frontend/src/lib/api.ts
  - apps/j16z-frontend/src/components/settings/alert-rules-tab.tsx
  - apps/j16z-frontend/src/components/deal-board.tsx
  - apps/j16z-frontend/src/components/ui/data-age-badge.tsx
  - apps/api/src/tests/alert-rules.test.ts
autonomous: true
requirements:
  - ALERT-04
  - ALERT-05
  - MKT-04

must_haves:
  truths:
    - "User can create, read, update, and delete alert rules via API"
    - "Per-deal alert threshold overrides work (dealId-scoped rules)"
    - "Every event creation across all pipelines enqueues an alert_evaluate job"
    - "Deal board shows spread with data-age badge (green/yellow/red)"
    - "AlertRulesTab wired to real API (not localStorage)"
  artifacts:
    - path: "apps/api/src/routes/alert-rules.ts"
      provides: "CRUD for alert rules"
      exports: ["alertRulesRoutes"]
    - path: "apps/j16z-frontend/src/components/ui/data-age-badge.tsx"
      provides: "Data-age indicator component"
      exports: ["DataAgeBadge"]
  key_links:
    - from: "apps/api/src/agency/event-factory.ts"
      to: "apps/api/src/queues/connection.ts"
      via: "ingestionQueue.add('alert_evaluate', ...)"
      pattern: "alert_evaluate"
    - from: "apps/j16z-frontend/src/components/settings/alert-rules-tab.tsx"
      to: "apps/j16z-frontend/src/lib/api.ts"
      via: "getAlertRules, createAlertRule, updateAlertRule, deleteAlertRule"
      pattern: "AlertRule"
    - from: "apps/j16z-frontend/src/components/deal-board.tsx"
      to: "apps/j16z-frontend/src/lib/api.ts"
      via: "getLatestMarketSnapshot"
      pattern: "MarketSnapshot"
---

<objective>
Wire the alert and market data subsystems into the application: alert rule CRUD API, event-factory alert_evaluate enqueue across all pipelines, frontend AlertRulesTab connected to real API, and deal board spread display with data-age badge.

Purpose: Plans 01 and 02 built the alert worker and market poller in isolation. This plan connects them to the existing event pipeline and frontend, completing the end-to-end flow from event ingestion to analyst notification, and from market data to visual spread display.
Output: Full alert + market data flow working end-to-end.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-alert-delivery-market-data/05-CONTEXT.md
@.planning/phases/05-alert-delivery-market-data/05-01-SUMMARY.md
@.planning/phases/05-alert-delivery-market-data/05-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From apps/api/src/alerts/types.ts:
```typescript
export interface AlertEvaluateData {
  eventId: string;
  firmId: string;
  dealId: string | null;
  materialityScore: number;
  severity: 'CRITICAL' | 'WARNING' | 'INFO';
}
```

From apps/api/src/alerts/alert-worker.ts:
```typescript
export async function handleAlertEvaluate(job: Job<AlertEvaluateData>): Promise<void>;
```

<!-- From Plan 02 outputs -->
From apps/api/src/market/types.ts:
```typescript
export interface SpreadComputation {
  grossSpread: number;
  annualizedReturn: number;
  impliedConsideration: number;
}
```

From apps/api/src/routes/market-snapshots.ts:
```typescript
export const marketSnapshotsRoutes: Hono<AuthEnv>;
// GET /:dealId — returns snapshot history array
// GET /:dealId/latest — returns single latest snapshot or 404
```

<!-- Existing code to modify -->
From apps/api/src/agency/event-factory.ts:
```typescript
export async function createAgencyEvent(params: CreateAgencyEventParams): Promise<void>;
// Must add alert_evaluate enqueue after event insertion
```

From apps/api/src/db/schema.ts (alertRules):
```typescript
export const alertRules = pgTable('alert_rules', {
  id, firmId, dealId, userId, name, threshold, channels, webhookUrl, webhookSecret, isActive, ...timestamps
}, () => firmIsolationPolicies());
```

From apps/j16z-frontend/src/lib/api.ts:
```typescript
// Must add: getAlertRules, createAlertRule, updateAlertRule, deleteAlertRule
// Must add: getLatestMarketSnapshot
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alert rule CRUD API + event-factory wiring</name>
  <files>
    apps/api/src/routes/alert-rules.ts,
    apps/api/src/index.ts,
    apps/api/src/agency/event-factory.ts,
    apps/api/src/edgar/download.ts,
    apps/api/src/courtlistener/webhook.ts,
    apps/api/src/rss/poller.ts,
    apps/api/src/tests/alert-rules.test.ts
  </files>
  <action>
    1. Create `apps/api/src/routes/alert-rules.ts` — Hono router with AuthEnv:
       - GET `/` — list alert rules for firm (firmId from auth). Include dealId filter as optional query param.
       - GET `/:id` — get single rule by ID, scoped to firm
       - POST `/` — create alert rule. Validate with zod: name (string, 1-100), threshold (integer, 0-100), channels (array of 'email'|'slack'|'webhook'), dealId (optional uuid), webhookUrl (optional string URL), isActive (optional boolean, default true). If channels includes 'webhook' and webhookUrl is provided, generate webhookSecret: `crypto.randomBytes(32).toString('hex')`. Return created rule WITH webhookSecret (only time it's returned).
       - PATCH `/:id` — update rule. Same validation but all fields optional. Do NOT allow updating webhookSecret via PATCH.
       - DELETE `/:id` — soft delete (set deletedAt). Scoped to firm.
       - POST `/:id/test` — test delivery. Send a test notification to all configured channels using a fake event. Useful for verifying Slack webhook URL works.

    2. Mount alertRulesRoutes at `/api/alert-rules` in index.ts (after auth middleware, alongside other routes).

    3. Wire alert_evaluate into event creation across ALL pipelines. In each location where an event is inserted into the DB, add an `ingestionQueue.add('alert_evaluate', { ... }, { delay: 5000 })` call after the insert:

       a. `apps/api/src/agency/event-factory.ts` — createAgencyEvent(): After the DB insert, import Queue from bullmq and ingestionQueue connection, enqueue alert_evaluate with { eventId, firmId, dealId, materialityScore, severity } from the just-inserted event. Import ingestionQueue from queues or create inline Queue instance.

       b. `apps/api/src/edgar/download.ts` — After edgar_download creates FILING events, enqueue alert_evaluate. Look for where events are inserted and add the enqueue call after.

       c. `apps/api/src/courtlistener/webhook.ts` — After COURT events are created, enqueue alert_evaluate.

       d. `apps/api/src/rss/poller.ts` — After NEWS events are created, enqueue alert_evaluate.

       For the queue reference, use a shared ingestion queue import. Check if `apps/api/src/queues/ingestion.ts` or similar exists; if not, create a small shared export: `export const ingestionQueue = new Queue('ingestion', { connection: redisConnection })` that can be imported by event factories. If an ingestionQueue is already exported from somewhere, use that.

    4. Create `apps/api/src/tests/alert-rules.test.ts`:
       - Mock adminDb
       - Test: GET / returns rules for firm
       - Test: POST / creates rule with generated webhookSecret when webhook channel included
       - Test: POST / without webhook channel does not generate webhookSecret
       - Test: PATCH /:id updates threshold and channels
       - Test: DELETE /:id soft-deletes
       - Test: Firm isolation — cannot access other firm's rules
  </action>
  <verify>
    <automated>cd /Users/arpan/Documents/j16z && pnpm test:be -- --run src/tests/alert-rules.test.ts && pnpm check</automated>
  </verify>
  <done>Alert rule CRUD API works. All 4 event pipelines (agency, EDGAR, CourtListener, RSS) enqueue alert_evaluate after event creation. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend wiring — AlertRulesTab + spread display + data-age badge</name>
  <files>
    apps/j16z-frontend/src/lib/api.ts,
    apps/j16z-frontend/src/lib/types.ts,
    apps/j16z-frontend/src/components/settings/alert-rules-tab.tsx,
    apps/j16z-frontend/src/components/ui/data-age-badge.tsx,
    apps/j16z-frontend/src/components/deal-board.tsx
  </files>
  <action>
    1. Add API functions to `apps/j16z-frontend/src/lib/api.ts`:
       - `getAlertRules(dealId?: string): Promise<AlertRule[]>` — GET /api/alert-rules?dealId=X
       - `createAlertRule(rule: CreateAlertRuleInput): Promise<AlertRule>` — POST /api/alert-rules
       - `updateAlertRule(id: string, updates: Partial<AlertRule>): Promise<AlertRule>` — PATCH /api/alert-rules/:id
       - `deleteAlertRule(id: string): Promise<void>` — DELETE /api/alert-rules/:id
       - `testAlertRule(id: string): Promise<void>` — POST /api/alert-rules/:id/test
       - `getLatestMarketSnapshot(dealId: string): Promise<MarketSnapshot | null>` — GET /api/market-snapshots/:dealId/latest
       - `getMarketSnapshots(dealId: string): Promise<MarketSnapshot[]>` — GET /api/market-snapshots/:dealId
       - Follow existing pattern: check USE_MOCK_DATA, return mock data or fetch from real API

    2. Add/update types in `apps/j16z-frontend/src/lib/types.ts` if needed:
       - `AlertRule` type: { id, name, threshold, channels, dealId, webhookUrl, isActive, createdAt, updatedAt }
       - `CreateAlertRuleInput`: { name, threshold, channels, dealId?, webhookUrl? }
       - Ensure MarketSnapshot type matches backend schema (currentPrice, targetPrice, grossSpread, annualizedReturn, volume, timestamp)

    3. Wire `AlertRulesTab` (`apps/j16z-frontend/src/components/settings/alert-rules-tab.tsx`) to real API:
       - Replace any localStorage-based rule storage with calls to getAlertRules/createAlertRule/updateAlertRule/deleteAlertRule
       - Use React Query (useQuery/useMutation) or useEffect+useState pattern matching existing component patterns
       - Keep existing UI (threshold sliders, channel toggles, per-deal overrides) — just wire the data layer
       - Add "Test" button per rule that calls testAlertRule(id)
       - Show webhookSecret once after creation (in a copyable field), then hide

    4. Create `apps/j16z-frontend/src/components/ui/data-age-badge.tsx`:
       - `DataAgeBadge` component accepting `lastUpdated: Date | null`
       - If null: show "No data" in muted text
       - Calculate ageMinutes = (Date.now() - lastUpdated.getTime()) / 60000
       - Color: green (<5 min), yellow (5-30 min), red (>30 min)
       - Label: "Live" (<1 min), "{N}m ago" (<60 min), "{N}h ago" (>=60 min)
       - Small rounded pill style matching existing badge patterns (Tailwind classes)
       - Add "Market Closed" variant when market is not open (check hours client-side using same logic as backend isMarketOpen)

    5. Update `apps/j16z-frontend/src/components/deal-board.tsx`:
       - For each deal card/row, fetch latest market snapshot using getLatestMarketSnapshot (or batch via getDeals if deal objects already include currentPrice + updatedAt)
       - Display gross spread percentage and annualized return next to deal name/symbol
       - Render DataAgeBadge with the latest snapshot's timestamp
       - If no snapshot data exists, show "--" for spread values
       - Follow existing deal board layout patterns — do not restructure the board
  </action>
  <verify>
    <automated>cd /Users/arpan/Documents/j16z && pnpm check && pnpm build</automated>
  </verify>
  <done>AlertRulesTab fetches/creates/updates/deletes rules via real API. Deal board shows spread + annualized return + DataAgeBadge. TypeScript compiles. Build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm check` passes
- `pnpm build` passes
- `pnpm test:be -- --run src/tests/alert-rules.test.ts` passes
- alert-rules routes mounted in index.ts
- All 4 event factories enqueue alert_evaluate
- AlertRulesTab uses API functions (not localStorage)
- DataAgeBadge component renders green/yellow/red based on age
</verification>

<success_criteria>
- Alert rule CRUD API supports create/read/update/delete with per-deal overrides
- All event pipelines (EDGAR, CourtListener, FTC/DOJ, RSS) trigger alert evaluation
- AlertRulesTab wired to real backend
- Deal board shows live spread data with data-age indicator
- Full end-to-end: event created -> alert_evaluate queued -> rules evaluated -> notification delivered
</success_criteria>

<output>
After completion, create `.planning/phases/05-alert-delivery-market-data/05-03-SUMMARY.md`
</output>
