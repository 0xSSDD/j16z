---
phase: 04-courtlistener-ftcdoj-rss-ingestion
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - apps/j16z-frontend/src/lib/api.ts
  - apps/j16z-frontend/src/components/settings/integrations-tab.tsx
  - apps/api/src/rss/poller.ts
  - apps/api/src/tests/courtlistener-poller.test.ts
autonomous: true
requirements:
  - RSS-03
  - RSS-01
  - RSS-02

must_haves:
  truths:
    - "Frontend IntegrationsTab displays all sources including CourtListener with correct icons and colors"
    - "PACER credential expiry warning appears in IntegrationsTab when <=30 days remain"
    - "RSS news items create events with relevance-based materiality scores (not hardcoded 40)"
    - "CourtListener poller has unit tests covering discovery, dedup, and event creation"
  artifacts:
    - path: "apps/j16z-frontend/src/lib/api.ts"
      provides: "Updated IntegrationHealth type with displayName and pacerCredential"
      contains: "displayName"
    - path: "apps/j16z-frontend/src/components/settings/integrations-tab.tsx"
      provides: "Source name resolution using displayName; PACER credential warning badge"
      contains: "pacerCredential"
    - path: "apps/api/src/rss/poller.ts"
      provides: "RSS news scoring with keyword-based materiality"
      contains: "materialityScore"
    - path: "apps/api/src/tests/courtlistener-poller.test.ts"
      provides: "Unit tests for CourtListener poller and webhook handler"
  key_links:
    - from: "apps/j16z-frontend/src/components/settings/integrations-tab.tsx"
      to: "apps/j16z-frontend/src/lib/api.ts"
      via: "getIntegrationHealth() -> displayName field"
      pattern: "integration\\.displayName"
    - from: "apps/api/src/rss/poller.ts"
      to: "apps/api/src/agency/event-factory.ts"
      via: "createAgencyEvent with variable materialityScore"
      pattern: "materialityScore.*score"
---

<objective>
Wire the frontend to display all sources (including CourtListener) with proper naming and PACER credential warnings. Close the RSS-03 gap by adding keyword-based materiality scoring to RSS news items. Add CourtListener unit tests.

Purpose: Without frontend wiring, the CourtListener integration is invisible to analysts. Without variable RSS scoring, all news items appear equally (un)important. Tests ensure CourtListener logic is regression-safe.
Output: Updated frontend IntegrationsTab, enhanced RSS scoring, CourtListener unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-courtlistener-ftcdoj-rss-ingestion/04-RESEARCH.md
@.planning/phases/04-courtlistener-ftcdoj-rss-ingestion/04-01-SUMMARY.md
@.planning/phases/04-courtlistener-ftcdoj-rss-ingestion/04-02-SUMMARY.md

@apps/j16z-frontend/src/lib/api.ts
@apps/j16z-frontend/src/components/settings/integrations-tab.tsx
@apps/api/src/rss/poller.ts
@apps/api/src/tests/deal-matcher.test.ts

<interfaces>
<!-- From 04-02: Updated integrations health response shape -->
Backend now returns:
```typescript
{
  sources: [{
    sourceName: string,
    displayName: string,  // NEW — human-readable name
    lastSuccessfulSync: string | null,
    lastError: string | null,
    lastErrorAt: string | null,
    itemsIngested: number,
    isHealthy: boolean,
  }],
  pacerCredential: {  // NEW
    isExpiringSoon: boolean,
    daysUntilExpiry: number | null,
    expiryDate: string | null,
    lastChanged: string | null,
  }
}
```

<!-- From 04-01: CourtListener exports -->
From apps/api/src/courtlistener/poller.ts:
```typescript
export async function handleCourtListenerPoll(job: Job): Promise<void>;
```
From apps/api/src/courtlistener/webhook.ts:
```typescript
export async function handleCourtListenerWebhook(job: Job): Promise<void>;
```
From apps/api/src/courtlistener/types.ts:
```typescript
export const courtListenerWebhookSchema: z.ZodType;
export const COURTLISTENER_API_BASE: string;
export const COURTLISTENER_IPS: Set<string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend integrations wiring and PACER credential display</name>
  <files>apps/j16z-frontend/src/lib/api.ts, apps/j16z-frontend/src/components/settings/integrations-tab.tsx</files>
  <action>
**api.ts** updates:
- Add `displayName` to `IntegrationHealth` interface: `displayName?: string`
- Add `PacerCredentialHealth` interface: `{ isExpiringSoon: boolean, daysUntilExpiry: number | null, expiryDate: string | null, lastChanged: string | null }`
- Add `IntegrationHealthResponse` interface: `{ sources: IntegrationHealth[], pacerCredential?: PacerCredentialHealth }`
- Update `getIntegrationHealth()` return type handling:
  - When `USE_MOCK_DATA=true`: return existing mock data (unchanged)
  - When `USE_MOCK_DATA=false`: fetch from `/api/integrations/health`, parse the `{ sources, pacerCredential }` response, map each source to `IntegrationHealth` using `displayName ?? sourceName` as the `source` field, and also return `pacerCredential`
  - Change return type to `Promise<{ sources: IntegrationHealth[], pacerCredential?: PacerCredentialHealth }>` OR keep returning `IntegrationHealth[]` and add a separate `getPacerCredentialHealth()` function. Simpler: update `getIntegrationHealth` to return `{ sources: IntegrationHealth[], pacerCredential?: PacerCredentialHealth }` and update callers.
- Update `MOCK_INTEGRATION_HEALTH` to include a CourtListener entry: `{ id: 'int-cl', source: 'CourtListener', status: 'healthy', lastSyncAt: ..., itemsIngested: 0, lastError: null, pollIntervalMinutes: 30 }`

**integrations-tab.tsx** updates:
- Update the `useEffect` and `refreshHealth` to handle the new response shape: `const { sources, pacerCredential } = await getIntegrationHealth()` — set `healthData` to `sources`, store `pacerCredential` in a new state variable
- Use `integration.source` which now comes from `displayName` (the api.ts layer maps it). The existing `SOURCE_ICONS` and `SOURCE_COLORS` maps already use display names ('SEC EDGAR', 'CourtListener', etc.) so they should match.
- Add a PACER credential warning section below the Data Source Health grid:
  - Only show when `pacerCredential?.isExpiringSoon` is true
  - Display: amber warning banner with text "PACER credentials expire in {daysUntilExpiry} days (expiry: {expiryDate}). Update at https://pacer.uscourts.gov"
  - Use `AlertTriangle` icon from lucide-react (already imported)
  - Styling: `rounded-lg border border-amber-500/20 bg-amber-500/5 p-4` with amber text
  </action>
  <verify>
    <automated>cd /Users/arpan/Documents/j16z && cd apps/j16z-frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>IntegrationsTab displays all sources with correct icons/colors using backend displayName; PACER credential expiry warning visible when <=30 days remain; CourtListener appears in both mock and real data modes</done>
</task>

<task type="auto">
  <name>Task 2: RSS news relevance scoring and CourtListener unit tests</name>
  <files>apps/api/src/rss/poller.ts, apps/api/src/tests/courtlistener-poller.test.ts</files>
  <action>
**rss/poller.ts** — Add keyword-based materiality scoring for RSS news items:
- Currently RSS items likely create events with a hardcoded `materialityScore: 40`. Update to use a `scoreRssItem(title: string, content: string)` function:
  - Base score: 30 (NEWS/RSS_ARTICLE)
  - Keyword boosts (additive, capped at 70):
    - "merger" / "acquisition" / "deal" -> +10
    - "antitrust" / "regulatory" / "FTC" / "DOJ" -> +15
    - "injunction" / "block" / "challenge" -> +20
    - "second request" / "HSR" -> +20
    - "termination" / "break fee" / "MAE" -> +15
    - "shareholder suit" / "litigation" -> +10
  - Cap at 70 (NEWS events should not reach CRITICAL on keywords alone)
- Update the event creation call to use `scoreRssItem(title, content)` instead of hardcoded 40
- Derive severity from score using same pattern: >=70 CRITICAL, >=50 WARNING, else INFO

**tests/courtlistener-poller.test.ts** — Unit tests for CourtListener integration:
- Follow existing test patterns (see `apps/api/src/tests/deal-matcher.test.ts` for mock patterns)
- Use vitest with vi.mock for adminDb and fetch
- Tests:
  1. `handleCourtListenerPoll creates COURT events for discovered dockets` — mock fetch to return search results with one docket, verify createCourtEvent called
  2. `handleCourtListenerPoll skips already-subscribed dockets` — mock events table to have existing event with courtlistenerDocketId in metadata, verify no duplicate subscription
  3. `handleCourtListenerWebhook creates events from webhook payload` — mock webhook payload with docket entries, verify createCourtEvent called with correct subType
  4. `handleCourtListenerWebhook deduplicates by sourceGuid` — mock existing event with sourceGuid, verify no duplicate creation
  5. `getCourtMaterialityScore returns correct scores by subType` — test injunction=90, motion=70, docket_entry=50
  6. `scoreRssItem boosts score for M&A keywords` — test various keyword combinations
- Mock patterns: use `vi.mock('../db/index.js')` and `vi.mock('../courtlistener/event-factory.js')` to isolate
  </action>
  <verify>
    <automated>cd /Users/arpan/Documents/j16z && pnpm test:be -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>RSS items scored by keyword relevance (30-70 range); CourtListener poller tests cover discovery, dedup, webhook processing, and materiality scoring; all backend tests pass</done>
</task>

</tasks>

<verification>
- `pnpm test:be -- --run` passes (all existing + new tests)
- Frontend TypeScript compiles: `cd apps/j16z-frontend && npx tsc --noEmit`
- IntegrationsTab shows CourtListener source card with violet icon
- RSS news items get variable scores based on content keywords
</verification>

<success_criteria>
- Frontend IntegrationsTab correctly displays all 5 source types with icons and colors
- PACER credential warning renders when daysUntilExpiry <= 30
- RSS news items scored 30-70 based on M&A keyword relevance, not hardcoded 40
- CourtListener has 6+ unit tests covering core logic paths
- All tests pass, both frontend and backend TypeScript compile clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-courtlistener-ftcdoj-rss-ingestion/04-03-SUMMARY.md`
</output>
