---
phase: 07-frontend-refinement-exports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/j16z-frontend/src/components/landing-page.tsx
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "Landing page leads with analyst pain point, then presents j16z as the solution"
    - "Hero headline reads 'Deal intelligence, made faster.'"
    - "Landing page has 5+ sections: Hero, Feature showcase, How it works, Data sources, Pricing, CTA"
    - "Pricing section says 'Currently in beta — reach out for pricing' (not a tier grid)"
    - "Feature showcase uses rendered React component mockups (not static screenshots)"
    - "Overall aesthetic is Linear x Harvey: clean, dark, crafted — no AI-generated feel"
  artifacts:
    - path: "apps/j16z-frontend/src/components/landing-page.tsx"
      provides: "Polished landing page with 5+ sections and Linear x Harvey aesthetic"
      contains: "Deal intelligence, made faster"
  key_links:
    - from: "apps/j16z-frontend/src/components/landing-page.tsx"
      to: "apps/j16z-frontend/src/app/page.tsx"
      via: "default page import"
      pattern: "LandingPage"
---

<objective>
Polish the landing page to convey "deal intelligence, made faster" with a Linear x Harvey aesthetic — problem-first messaging, 5+ sections, rendered product mockups, and a beta pricing section.

Purpose: First-time visitors understand j16z's value proposition instantly. The landing page communicates craft and professionalism — every pixel intentional, no AI-generated feel. This is the front door for pilot client acquisition.
Output: Refined landing-page.tsx with Hero, Feature Showcase, How It Works, Data Sources, Pricing, and CTA sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-frontend-refinement-exports/07-CONTEXT.md
@.planning/phases/07-frontend-refinement-exports/07-RESEARCH.md

@apps/j16z-frontend/src/components/landing-page.tsx
@apps/j16z-frontend/src/app/globals.css

<interfaces>
<!-- Existing landing-page.tsx (758 lines) has: PolymarketCard, SearchResultsCard, SynthesisTerminal, FeatureCard components -->
<!-- Existing Aurora theme variables: --aurora-primary (amber), --aurora-secondary (indigo), --aurora-tertiary (pink) -->
<!-- Existing animate utilities: animate-float, animate-pulse-slow from tw-animate-css in globals.css -->
<!-- Do NOT add new @keyframes — use only existing animate- utilities from globals.css -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure landing page with 6 sections and problem-first messaging</name>
  <files>apps/j16z-frontend/src/components/landing-page.tsx</files>
  <action>
    Rewrite landing-page.tsx to deliver 6 sections in this order. Preserve the existing dark theme, Aurora accent colors, and font system (Plus Jakarta Sans headings, Inter body). Reuse and refine existing mock components (PolymarketCard, SearchResultsCard, SynthesisTerminal) rather than rebuilding — these ARE the "live product mockups" per CONTEXT.md.

    **Section 1 — Hero:**
    - Headline: "Deal intelligence, made faster." (h1, Plus Jakarta Sans, large, text-text-primary)
    - Subheadline: "Stop chasing filings across EDGAR, CourtListener, and FTC press releases. j16z turns fragmented M&A data into analyst-ready intelligence — automatically."
    - Two CTA buttons: "Get Started" (primary, amber bg) → /login, "See How It Works" (secondary, outline) → smooth scroll to How It Works section
    - Subtle aurora gradient background effect (use existing CSS variables, not new keyframes)
    - Clean, minimal — no animation beyond existing animate-float on decorative elements

    **Section 2 — Feature Showcase:**
    - Section heading: "Everything an M&A desk needs, in one terminal"
    - 3-4 rendered product mockups showing: (a) Inbox with materiality-scored events, (b) Deal card with spread + terms, (c) Alert delivery (email preview), (d) Memo editor
    - Refine existing PolymarketCard / SearchResultsCard / SynthesisTerminal components or create new inline mockup components that look like actual product UI screenshots
    - Each mockup has a label/caption below explaining the feature
    - Use a grid layout (2 cols on lg, 1 on mobile) with generous spacing

    **Section 3 — How It Works:**
    - id="how-it-works" for scroll target
    - 3-step flow: (1) "Connect your deal universe" — add CIKs, watchlists, RSS feeds, (2) "We monitor everything" — EDGAR, CourtListener, FTC/DOJ, market data, (3) "Get intelligence, not noise" — scored events, alerts, memos, exports
    - Each step: number (large, amber), heading, description, subtle connecting line between steps
    - Clean horizontal layout on desktop, vertical on mobile

    **Section 4 — Data Sources:**
    - Section heading: "Built on authoritative sources"
    - Grid of 5 sources: SEC EDGAR, CourtListener, FTC/DOJ Antitrust, Market Data, Curated RSS
    - Each source card: icon/logo placeholder, name, 1-line description of what j16z extracts
    - Muted styling — informational, not flashy

    **Section 5 — Pricing:**
    - Section heading: "Pricing"
    - Single card with text: "Currently in beta — reach out for pricing"
    - "Contact us" button → mailto:hello@j16z.com or similar
    - No tier grid, no pricing numbers — per locked decision

    **Section 6 — CTA Footer:**
    - "Ready to stop chasing filings?" or similar closing hook
    - "Get Started" button → /login
    - Footer with minimal links (Privacy, Terms, GitHub)

    **Styling rules:**
    - Use ONLY existing CSS variables (--bg-primary, --bg-surface, --text-primary, --text-muted, --aurora-primary, etc.)
    - Use ONLY existing animate- utilities from globals.css (animate-float, animate-pulse-slow) — do NOT add new @keyframes
    - Generous whitespace between sections (py-24 or py-32)
    - Max-width container (max-w-6xl mx-auto px-6)
    - Responsive: stack to single column on mobile
    - Dark mode is default; light mode should also work (use CSS variables, not hardcoded colors)
  </action>
  <verify>
    <automated>cd /Users/arpan/Documents/j16z && pnpm check 2>&1 | tail -20 && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <done>Landing page renders 6 sections with problem-first messaging, "Deal intelligence, made faster." hero, rendered product mockups, beta pricing section, and Linear x Harvey aesthetic. TypeScript and Biome pass.</done>
</task>

</tasks>

<verification>
1. `pnpm check` passes (TypeScript compiles)
2. `pnpm lint` passes (Biome formatting)
3. `pnpm build` completes without errors
4. Landing page at `/` renders all 6 sections
5. Hero says "Deal intelligence, made faster."
6. Pricing section shows beta messaging (no tier grid)
7. Page looks clean and crafted on both desktop and mobile viewport widths
</verification>

<success_criteria>
- Landing page conveys "deal intelligence, made faster" with Linear x Harvey aesthetic
- 5+ sections present: Hero, Feature Showcase, How It Works, Data Sources, Pricing, CTA
- Problem-first messaging hierarchy: pain point then solution
- Pricing says "Currently in beta — reach out for pricing"
- Feature showcase uses rendered React mockup components (not static images)
- No new CSS keyframes added — uses only existing animate- utilities
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-refinement-exports/07-02-SUMMARY.md`
</output>
