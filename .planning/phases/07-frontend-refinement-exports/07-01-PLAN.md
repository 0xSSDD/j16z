---
phase: 07-frontend-refinement-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/j16z-frontend/src/components/deal-card.tsx
  - apps/j16z-frontend/src/components/deal-card/deal-card-header.tsx
  - apps/j16z-frontend/src/components/deal-card/tabs/terms-tab.tsx
  - apps/j16z-frontend/src/components/deal-card/tabs/events-tab.tsx
  - apps/j16z-frontend/src/components/deal-card/tabs/spread-history-tab.tsx
  - apps/j16z-frontend/src/components/deal-card/tabs/news-research-tab.tsx
  - apps/j16z-frontend/src/components/deal-card/tabs/reg-litigation-tab.tsx
  - apps/j16z-frontend/src/components/deal-card/deal-event-side-panel.tsx
  - apps/j16z-frontend/src/components/keyboard-help-modal.tsx
  - apps/j16z-frontend/package.json
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Deal page shows 5 horizontal tabs (Terms, Events, Spread History, News/Research, Reg & Litigation) below a sticky metrics header"
    - "Default tab is Terms when opening a deal card"
    - "Clicking an event row in the Events tab opens a slide-in sidebar from the right showing event detail"
    - "Tab content remains visible beside the sidebar when event detail is open"
    - "Pressing 1-5 keys switches between tabs; j/k navigates event list; Enter opens sidebar; Esc closes it"
    - "Sticky header shows deal name, price, spread, p_close, status — does NOT scroll away or change per tab"
    - "Onboarding flow remains minimal (existing OnboardingForm, no wizard)"
  artifacts:
    - path: "apps/j16z-frontend/src/components/deal-card.tsx"
      provides: "Refactored deal card orchestrator with Radix Tabs and sidebar state"
      contains: "Tabs.Root"
    - path: "apps/j16z-frontend/src/components/deal-card/deal-card-header.tsx"
      provides: "Extracted sticky metrics header"
      exports: ["DealCardHeader"]
    - path: "apps/j16z-frontend/src/components/deal-card/tabs/terms-tab.tsx"
      provides: "Terms tab with clause grouping (reused from existing deal-card.tsx)"
      exports: ["TermsTab"]
    - path: "apps/j16z-frontend/src/components/deal-card/tabs/events-tab.tsx"
      provides: "Events tab with j/k navigation and Enter-to-open sidebar"
      exports: ["EventsTab"]
    - path: "apps/j16z-frontend/src/components/deal-card/deal-event-side-panel.tsx"
      provides: "Event detail sidebar reusing InboxSidePanel pattern"
      exports: ["DealEventSidePanel"]
    - path: "apps/j16z-frontend/src/components/keyboard-help-modal.tsx"
      provides: "Updated keyboard shortcuts including deal card tab shortcuts"
      contains: "Deal Card"
  key_links:
    - from: "apps/j16z-frontend/src/components/deal-card.tsx"
      to: "apps/j16z-frontend/src/components/deal-card/tabs/events-tab.tsx"
      via: "Radix Tabs.Content rendering EventsTab"
      pattern: "Tabs\\.Content.*events"
    - from: "apps/j16z-frontend/src/components/deal-card.tsx"
      to: "apps/j16z-frontend/src/components/deal-card/deal-event-side-panel.tsx"
      via: "selectedEventId state driving sidebar visibility"
      pattern: "selectedEventId.*DealEventSidePanel"
    - from: "apps/j16z-frontend/src/components/deal-card/tabs/events-tab.tsx"
      to: "apps/j16z-frontend/src/components/deal-card.tsx"
      via: "onSelect callback to set selectedEventId"
      pattern: "onSelect"
---

<objective>
Refactor the deal card from collapsible sections to an eToro-style tabbed layout with Radix UI Tabs, add a CourtListener-style event detail sidebar, and wire keyboard shortcuts for tab switching and event navigation.

Purpose: Deal cards become scannable and keyboard-navigable — analysts can switch between Terms, Events, Spread History, News/Research, and Reg & Litigation without scrolling through a monolithic page. Event detail opens in context without losing the event list.
Output: Refactored deal-card.tsx with extracted tab components, sticky header, event sidebar, and keyboard shortcut wiring.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-refinement-exports/07-CONTEXT.md
@.planning/phases/07-frontend-refinement-exports/07-RESEARCH.md

@apps/j16z-frontend/src/components/deal-card.tsx
@apps/j16z-frontend/src/components/inbox/inbox-side-panel.tsx
@apps/j16z-frontend/src/components/keyboard-help-modal.tsx
@apps/j16z-frontend/src/lib/types.ts

<interfaces>
<!-- Reuse existing inbox side panel pattern for deal event detail -->
<!-- From inbox-side-panel.tsx: slide-in panel with event detail, close on Esc -->
<!-- From deal-card.tsx: ClauseCard, CLAUSE_CATEGORIES grouping, event timeline, spread chart sections — extract into tab components -->
<!-- From keyboard-help-modal.tsx: shortcut definitions array — extend with deal card shortcuts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract deal card header and create tab components</name>
  <files>
    apps/j16z-frontend/package.json,
    apps/j16z-frontend/src/components/deal-card/deal-card-header.tsx,
    apps/j16z-frontend/src/components/deal-card/tabs/terms-tab.tsx,
    apps/j16z-frontend/src/components/deal-card/tabs/events-tab.tsx,
    apps/j16z-frontend/src/components/deal-card/tabs/spread-history-tab.tsx,
    apps/j16z-frontend/src/components/deal-card/tabs/news-research-tab.tsx,
    apps/j16z-frontend/src/components/deal-card/tabs/reg-litigation-tab.tsx,
    apps/j16z-frontend/src/components/deal-card/deal-event-side-panel.tsx
  </files>
  <action>
    1. Install @radix-ui/react-tabs in apps/j16z-frontend: `pnpm add @radix-ui/react-tabs`

    2. Create `deal-card-header.tsx`: Extract the sticky metrics header from the top of deal-card.tsx. Show deal name, acquirer/target, price, spread, p_close_base, status badge. Use `sticky top-0 z-10 bg-bg-primary border-b border-border` for stickiness. This header does NOT change per tab.

    3. Create `tabs/terms-tab.tsx`: Move the clause grouping logic from deal-card.tsx — reuse CLAUSE_CATEGORIES constant and ClauseCard component. Accept `clauses: Clause[]` and `deal: Deal` as props. Include the key terms section (termination fees, MAE, regulatory covenants) exactly as currently rendered.

    4. Create `tabs/events-tab.tsx`: Accept `events: Event[]`, `focusedIndex: number`, and `onSelect: (id: string) => void`, `onFocusChange: (index: number) => void` props. Render event rows (reuse EventTimeline or similar pattern from deal-card). Highlight the focused row with a ring/bg color. On row click, call `onSelect(event.id)`. The j/k and Enter keyboard handling is done in the parent (deal-card.tsx), not here — this component just renders and accepts callbacks.

    5. Create `tabs/spread-history-tab.tsx`: Move the SpreadChart rendering from deal-card.tsx. Accept `dealId: string` and `marketSnapshots: MarketSnapshot[]`. Lazy-load: only mount the chart when this tab is active (parent passes a `isActive` prop or this is handled by Radix unmounting inactive tabs — use Radix default behavior which unmounts inactive content).

    6. Create `tabs/news-research-tab.tsx`: Move the NewsSection from deal-card.tsx. Also include MemoList integration (import from existing memo components). Accept `deal: Deal`, `newsItems: NewsItem[]`.

    7. Create `tabs/reg-litigation-tab.tsx`: Filter events by type COURT and AGENCY. Show court cases and agency events in grouped sections. Accept `events: Event[]`, `deal: Deal`.

    8. Create `deal-event-side-panel.tsx`: Reuse the InboxSidePanel pattern from `apps/j16z-frontend/src/components/inbox/inbox-side-panel.tsx`. Accept `eventId: string`, `onClose: () => void`, `events: Event[]`. Find the event by ID from the events array. Show event detail (type, subtype, materiality badge, summary, source link, timestamp). Panel slides in from right with `fixed right-0 top-0 h-full w-[400px]` positioning and transition animation. Close on Esc (handled by parent) or close button click.
  </action>
  <verify>
    <automated>cd /Users/arpan/Documents/j16z && pnpm check 2>&1 | tail -20</automated>
  </verify>
  <done>All 7 new component files exist, export their components, and TypeScript compiles without errors. @radix-ui/react-tabs is installed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire deal-card.tsx with Radix Tabs, sidebar state, and keyboard shortcuts</name>
  <files>
    apps/j16z-frontend/src/components/deal-card.tsx,
    apps/j16z-frontend/src/components/keyboard-help-modal.tsx
  </files>
  <action>
    1. Rewrite `deal-card.tsx` to use Radix Tabs in controlled mode:
       - Import `* as Tabs from '@radix-ui/react-tabs'`
       - Define `const TABS = ['terms', 'events', 'spread-history', 'news-research', 'reg-litigation'] as const`
       - State: `activeTab` (default 'terms'), `selectedEventId` (null), `focusedEventIndex` (0)
       - Layout structure:
         ```
         <div className="flex flex-col h-full">
           <DealCardHeader deal={deal} />  <!-- sticky, outside tabs -->
           <div className="flex flex-1 overflow-hidden">
             <div className={`flex-1 overflow-y-auto ${selectedEventId ? 'pr-[400px]' : ''}`}>
               <Tabs.Root value={activeTab} onValueChange={setActiveTab}>
                 <Tabs.List> ... 5 triggers with styling ... </Tabs.List>
                 <Tabs.Content value="terms"><TermsTab /></Tabs.Content>
                 <Tabs.Content value="events"><EventsTab /></Tabs.Content>
                 ... etc ...
               </Tabs.Root>
             </div>
             {selectedEventId && <DealEventSidePanel ... />}
           </div>
         </div>
         ```
       - Tab trigger styling: `px-4 py-2 text-sm font-medium text-text-muted hover:text-text-primary data-[state=active]:text-text-primary data-[state=active]:border-b-2 data-[state=active]:border-aurora-primary transition-colors`

    2. Add keyboard shortcut useEffect:
       - Guard: `if (e.target instanceof HTMLInputElement || e.target instanceof HTMLTextAreaElement || e.target instanceof HTMLElement && e.target.getAttribute('contenteditable')) return;`
       - Keys 1-5: `setActiveTab(TABS[parseInt(e.key) - 1])` with bounds check
       - j/k: Only when activeTab === 'events'. j increments focusedEventIndex (clamped), k decrements
       - Enter: Only when activeTab === 'events'. Sets selectedEventId from events[focusedEventIndex]
       - Esc: Sets selectedEventId to null (closes sidebar)
       - Dependencies: [activeTab, events, focusedEventIndex]

    3. Preserve ALL existing data fetching logic (api.getDeals, api.getClauses, api.getMarketSnapshots, etc.) — do not change how data is loaded, only how it's rendered.

    4. Preserve the existing CSV export function and memo tab integration — move the export button into the header or keep it accessible.

    5. Update `keyboard-help-modal.tsx`: Add a "Deal Card" section to the shortcuts array:
       - `1-5` → "Switch tab (Terms, Events, Spread, News, Reg)"
       - `j / k` → "Navigate events (Events tab)"
       - `Enter` → "Open event detail"
       - `Esc` → "Close event detail"
  </action>
  <verify>
    <automated>cd /Users/arpan/Documents/j16z && pnpm check 2>&1 | tail -20 && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <done>Deal card renders with 5 horizontal tabs below a sticky header. Terms tab is default. Events tab supports j/k/Enter/Esc navigation. Sidebar opens on event selection. Keyboard help modal includes deal card shortcuts. TypeScript and Biome pass.</done>
</task>

</tasks>

<verification>
1. `pnpm check` passes (TypeScript compiles)
2. `pnpm lint` passes (Biome formatting and linting)
3. `pnpm build` completes without errors
4. Deal card page (`/app/deals/[id]`) renders with 5 tabs
5. Sticky header stays fixed while scrolling tab content
6. Events tab event click opens side panel
7. Keys 1-5 switch tabs, j/k navigate events, Enter opens detail, Esc closes
</verification>

<success_criteria>
- Deal page shows tabbed layout with Terms as default tab
- Event detail sidebar opens from Events tab without page navigation
- Keyboard shortcuts work for tab switching and event navigation
- Sticky header shows deal metrics across all tabs
- Onboarding flow unchanged (minimal, firm creation only)
- All existing deal card functionality preserved (data fetching, export, memo)
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-refinement-exports/07-01-SUMMARY.md`
</output>
